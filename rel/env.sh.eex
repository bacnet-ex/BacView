#!/bin/sh

export RELEASE_DISTRIBUTION=none
#if [ -n "$RELEASE_ROOT" ]; then
#  export RELEASE_ROOT
#  export RELEASE_MODE=release
#  export RELEASE_NAME="${RELEASE_NAME:-<%= @release.name %>}"
#fi

export PHX_SERVER="${PHX_SERVER:-true}"
export PORT="${PORT:-4000}"

if [ -z "$SECRET_KEY_BASE" ] && [ -f "$RELEASE_ROOT/.env" ]; then
  set -a
  . "$RELEASE_ROOT/.env"
  set +a
fi
